# Pricing-component-with-toggle using Webpack
Initial commands:

npm init -y

npm i -D webpack webpack-cli

— development mode:

npm run dev

— production mode:

npm run build

— open server:

npm run serve
